# 作業メモ（kanji-print-app 実装担当）

## 2026-09-05

### 完了：バージョンアップで進捗記録が消える問題を修正（48a14d2）

**原因**（バグではなく保存キーの改名）
進捗記録の localStorage キーを過去4回改名していたため、そのたびに旧キーの記録が
読まれなくなっていた。記録自体はブラウザ内に残っている。

    kanji_app_settings_mastery_v1 → kanji_app_mastery_status_v1
    → kanji_app_mastery_simple_v1 → kanji_app_mastery_v2 → kq_kanji_stats_v1（現行）

a00513d（kosukequiz-battleからの移植）ではキー名に加えて
- id体系 `g1_01` → `k1_01`
- 記録形式 `{passed, lastDate}` → `{correct, wrong, box, nextDue}`
も同時に変わっていた。語が一致する30語のうち**28語で連番がずれている**ため、
id の読み替えでは復元できず、語(word)で突き合わせる必要があった。
なお a00513d 以降のデータ追加は追記のみで、id の付け替わりは0件。

**対応**
- `migrateLegacyMastery()`：起動時に旧キー4種を現行キーへ取り込む（初回のみ・非破壊）。
  旧キーは削除しないので元データは保全。既に記録がある語は上書きしない
- `convertLegacyStat()`：旧形式 `{passed, lastDate}` を間隔反復形式へ変換
- `rescueOrphanedRecords()` + `kq_kanji_stats_words_v1`（語の控え）：
  将来 id が振り直されても記録を現行 id へ復帰させる再発防止
- 共有キー `kq_kanji_stats_v1` の形式自体は変更なし → kosukequiz-battle との互換維持
- 履歴リセット後に旧キーから復活しないようフラグを立てる
- README に「キー名を改名しない・id を振り直さない」を経緯つきで明記

**検証**：Chromium で旧データを仕込んで18項目、全て通過
（引き継ぎ／連番ずれの付け替え／二重取り込み防止／id振り直し復帰／
姉妹アプリ由来 id の保全／リセット後の非復活／通常表示の回帰）

### 完了：夏期講習 復習編5〜8 の256語を取り込み

司令塔（claude-1c）からkosukequiz-battle担当（claude-4c）経由で依頼。
`G:\マイドライブ\四谷大塚\5年下\quiz_csv_夏期講習` の
`夏期講習_復習編N_漢字.json`（N=5〜8）をGoogleドライブから取得して反映。

- 復習編5 近畿地方 54語 / 復習編6 中部地方 71語 /
  復習編7 関東地方 58語 / 復習編8 東北地方／北海道地方 73語 = 256語
- 納品は旧形式（`genre` に単元名、`unit` は空）だったので、こちらで新スキーマへ変換
- id は kaki5_01〜 / kaki6_01〜 / kaki7_01〜 / kaki8_01〜。既存242語のidは一切変更なし
- KANJI_DATA 242→498語、KANJI_UNITS 6→10単元
- 検証：id重複0／count整合OK／必須フィールド欠落0／新規256語はmeanへの答え露出0

**重複について**：既存242語と15件、今回4ファイル間で7語が重複。ただし
既存データにも単元またぎの重複が4語（豪族・福岡県・仏教・促成栽培）あり、
復習編は地域別の復習単元なので同じ語が複数単元に出るのは想定内と判断し、
納品どおり全件取り込んだ。単元を複数選ぶと同じ語が2回出題される点は、getPool() で語の重複を除いて対応済み
（データは納品どおり残し、出題側で除く方式）。

### 完了：idの付け替わりで記録が別の語に移る不具合を修正

司令塔経由でkosukequiz-battle担当（claude-4c）から「idと順序の取り違え事故」の
警告を受け、こちらでも再現テストしたところ**同じ事故が起きることを確認**した。

- 元データJSONに `id` が無く、配列の位置から `kaki5_01`… と採番している
- 語を途中に足して再取り込みすると以降のidが1つずつずれる
- 旧 `rescueOrphanedRecords()` は「idが消えた場合」しか見ておらず、
  「idはあるが中身が別の語になった場合」を検出できていなかった
  → 〇✕が黙って隣の語のものになる

修正：起動時に `kq_kanji_stats_words_v1`（id→語の控え）と現在の語を突き合わせ、
食い違っていたら語をたよりに正しいidへ記録を移す。控えは記録が無いidぶんを
削除するようにして、古い控えによる誤判定も防いだ。

**本筋は元データJSONに安定したidを持たせること。** quiz_csv側へ要望済み。

### 完了：第4回（平安時代）46語を取り込み

「保元」「平治」の表記はユーザー判断で**テキスト準拠＝納品どおり**に決定。
根拠：noteに出典（演習問題集30P まとめ35／38）があり、読みが「ほうげん」
「へいじ」で答え「保元」「平治」と対応しているため、テキストの解答欄が
その形だと判断できる。「平将門の乱」等と粒度が違うのは、テキストの
解答欄の切り方の違いであって不具合ではない。

- KANJI_DATA 498→544語、KANJI_UNITS 10→11単元
- 時代順になるよう第3回の直後に挿入（idは k4_01〜k4_46 で新規採番）
- 既存の重複4語（摂政・荘園・遣唐使・厳島神社）は重複除去で対応済み

**あわせて getPool() を改良**：語の重複を除くとき、〇✕が既についている
方を優先するようにした。データの途中に単元を足すと採用されるidが
入れかわり、進捗が消えたように見えるため（厳島神社・平泉で実際に起きた）。

### 完了：重要度でしぼる機能（データを消さずに練習量を減らす）

「受験で漢字を聞かれそうな語に絞りたい、そんなに練習する時間がない」との要望。
当初はIssue経由でデータから36語を削除する流れで進めたが（Issue #1 → PR #2）、
消した内訳を priority/level と突き合わせたところ、
- level=標準の71%・発展の50%を削除（難しい語ほど多く消えていた＝目的と逆）
- priority=高かつlevel=基礎の18語が対象に入っていた
  （中臣鎌足・厩戸皇子・行基・卑弥呼・小野妹子など、漢字書き取りの定番）
という偏りがあった。

そこで、消すのではなく**出題を絞る**方式を提案し、採用された。
- 「高だけ／高＋中／すべて」を切りかえるだけ。KANJI_DATAは544語のまま
- 出題・印刷・進捗バーに効く。進捗一覧には全語が出るので状況は見える
- 設定は保存され、「出さない」とも併用できる
- 元データにもkosukequiz-battle側にも手を入れる必要がない＝巻き戻りの心配なし

**PR #2 はマージしない方針。** 判断が変わったら閉じること。

### 次にやること

- **第4回の漢字データ取り込み待ち**。quiz_csv 側へ納品形式を回答済み（結論は下記）
  - `第4回_漢字.json` は新スキーマ `{id, unit, unitKey, word, kana, mean, kai, priority, level}` で納品してもらう
  - 旧形式で来た場合は第4回に限りこちらで変換する
  - `unit` の単元名（`第4回.〇〇`）は予習シリーズの実タイトル。quiz_csv 側が決定
  - `KANJI_UNITS` への `{ key, name, count }` 追加はこちら側の担当
  - **id は末尾に追記するだけにすること**（振り直すと記録が壊れる）
- README のデータ更新方法に `unitKey` / `kai` / `priority` / `level` の説明が無く、
  quiz_csv 側から2往復の照会が発生した。追記しておくとよい
- 現行データは全 **498語**（第1回40／第2回43／第3回40／夏期復習編2の30／同3の52／同4の37／同5の54／同6の71／同7の58／同8の73）
- `kai` / `priority` / `level` は index.html のロジックでは未使用
  （kosukequiz-battle とのスキーマ共通化のためのメタデータ）

### 環境メモ
- ビルド不要の静的アプリ。`index.html` をブラウザで開くだけ
- 検証は Chromium（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）+ playwright-core で可能

## 2026-09-19 claude-c8 [69cec9]
- ローカルを origin/master（c191966）へ早送り。未コミットは ROLE.md 冒頭の注記（起動場所の案内13行）のみ、残してある
- **社会第4回46語の取り込み依頼 → origin 側で取り込み済みだった**（e89ecaf）。k4_01〜k4_46 を元データ `5年下\quiz_csv\第4回_漢字.json`（sha256 c373df12…3d8e 一致）と全項目照合し不一致0。KANJI_UNITS 第4回 count 46、公開版にも46件。55語削除の影響なし。変更・push 不要
- PR #6／ISSUE #5（第3回の13語削除）はユーザー判断（司令塔経由）でマージせずクローズ、ブランチ remove-kanji/issue5 削除済み。13語は残す
- 現行データは全489語

## 2026-09-20 claude-c8 [69cec9]｜依頼: 最重要10を親が選ぶ／正解した日だけ入れる
- 依頼書: `司令塔\回答\漢字プリント_最重要10と正解日のUI_依頼_2026-09-20.md`（この件だけ）
- 現状調査ずみ: 記録は `kq_kanji_stats_v1`（battle と共有・改名禁止）、印刷は getPool→lastCorrectAt 昇順→先頭n語の自動選出、「出さない」は `kanji_app_excluded_v1` に unitKey+word で保存、mean は全語にあり一覧に出せる
- 案を提出: `kanji-print-app\UI案_最重要10と正解日_2026-09-20.md`（①一覧 ②選んだ語だけ印刷＝新キー `kanji_app_picked_v1` ③⭕のみ＋日付入力）
- **判断待ち（司令塔経由でユーザーへ）**: (a) 正解日は最後の1回だけ=案A／全部ためる=案B、(b)「苦手な問題だけ」を残すか隠すか
- ユーザー判断（09-20・司令塔経由）: **案A（正解日は最後の1回だけ）／「苦手な問題だけ」は隠す**。この形で実装した
- **ローカルにコミット済み `47e6dff`（push はまだ。ユーザー承認後に司令塔が実行）**
  - 一覧タブ③（回を1つ選ぶ・意味・正解日）／チェックした語だけ印刷（新キー `kanji_app_picked_v1`、unitKey+word 保存）／日付＋⭕のみ（採点画面の❌も撤去）
  - `kq_kanji_stats_v1` はキー名も形も据え置き。過去の記録は削除していない（wrong も残したまま、画面に出さないだけ）
  - 印刷見出しの「範囲」を、選んだ語の回だけにする `printedUnitNames()` を追加（全単元が並ぶ不具合を修正）
  - 検査: `selftest_えらぶ機能.html`（Chrome headless で開くと20項目・全PASS）。印刷はPDF化して目視ずみ
- ⚠️ 未コミットの `ROLE.md`（起動場所の注記）はそのまま。これは Drive 側の更新分

## 2026-09-23｜依頼: 「出す語を選ぶのが手間」（★調べて報告まで。まだ何も作っていない）
- 依頼書: `司令塔\回答\漢字プリント_選ぶのが手間_依頼_2026-09-23.md`（この件だけ）
- 着任時の状態: ローカル HEAD = origin/master = `47e6dff`（9/20 の「選んだ語だけ」は push 済みだった）

### 結論: 依頼書の見立ては成立する。★新しく作らなくても、いまの画面でできる
本物の Chrome（`channel: chrome`・スマホ幅 390×844）を playwright で開き、**UIのクリックだけ**で確認。NG 0件。
検査: `tools\実機確認_選ぶ手間_2026-09-23.js`（結果 `tools\実機確認_選ぶ手間_2026-09-23_結果.txt`）

| 確かめたこと | 実測 |
|---|---|
| 1回目に出た10語 | プレビュー10語・紙（`#print-region`）10語。**全部 priority=高／全部 correct=0**（記録と突き合わせ） |
| ⭕を付けた翌日 | 10語。**1回目と1語も重ならない** |
| 開き直したあと | 設定が残り、**押す前から次の10語ができている**。⭕済みは入らない |
| 全部クリアしたとき | 0語／進捗「🎉 全問クリア達成！」／`alert` は「フィルタの条件に合う語がありません」 |
| 9/20 の「選んだ語だけ」 | 壊れていない（73語チェック→73語印刷） |

### ★検査が鳴ることの確認（確認ポイント 4-1）
`tools\実機確認_対照_2026-09-23.js`。「ひととおり終わった」状態を作り、
**正解ずみを除く OFF なら古い10語が戻ってくる（重なり10語）／ON なら0語**を確認。
→ 本検査の「1語も重なっていない」は空振りではない。
⚠️ 最初の対照は**仕込みに失敗していた**（`setCount()` は `currentSet` を空にするだけで作り直さない。
day1 が `[]` のまま「重なり0」と出ていた）。**「壊せたこと」を先に確かめる**のが要る実例。

### ユーザーにそのまま渡せる手順（司令塔へ報告ずみ）
1. 「① 印刷」タブ →「いままでどおり自動でえらぶ」（既定のまま）
2. **「クリア」を押してから、今週の回だけを押す**（★既定は全11単元が選ばれている＝`index.html:694`）
3. 「高だけ」「正解ずみを除く」「10語」を**1回だけ**押す（以後ずっと保存される）
4. 「🔄 別の問題に出題し直す」→「🖨️ ① テストプリント」
5. 書けたら「② 採点」で「✨ すべてできた」（できなかった語は押さない）
6. 次の週は**開き直すだけ**。まだ⭕のついていない語から自動で次の10語

### ★足りないところ（小さい案。**作る前に止めて報告した**）
1. **採点したあと、その画面のまま「① 印刷」を押すと、同じ10語がもう一度出る**（実測）。
   開き直せば直る。「🔄 別の問題に出題し直す」が小さい文字リンクなのが原因
   → 最小の直し: 採点を保存したら自動で次の10語にする
2. 全クリアしたときの `alert`「フィルタの条件に合う語がありません。フィルタを見直してください。」が**失敗のような文面**
3. 「高だけ」はほとんど絞らない（489→372語／第4回は46→38語）。手間を減らしているのは「正解ずみを除く＋10語」のほう
4. **「正解ずみを除く」は、一度⭕にした語を二度と出さない**（`isMasteredStat` は `correct>0`。`box`/`nextDue` は使っていない）。
   ＝復習は回らない。今回の目的（新出語を毎週回す）には合っているが、仕様として記録しておく

### ★依頼書の数字の訂正
依頼書の「114語」は**通常回4回ぶんだけ**（27+21+20+46）。
実データは **489語・11単元**で、夏期講習 復習編2〜8 の 375語が抜けている（`tools\count_data.js` で実測）。

### ★依頼と別に見つかった不具合（作業前に報告ずみ・まだ直していない）
**テストプリントのヒント（`mean`）に、同じ紙の別の問題の答えがそのまま印刷されている。**
実際に印刷した第4回の1枚目で、**5番のヒントに「太政大臣」（6番の答え）／6番のヒントに「平清盛」（5番の答え）**。
`tools\答えの露出をさがす.js`（自己テスト内蔵）で実測: **紙41枚のうち25枚（61%）・のべ57語**。
確認ポイント 1-2 の経路1。直し方はアプリ側（同じ紙に載せない）とデータ側（`mean` の書きかえ）の2通り。**指示待ち**。

### 証拠
- 紙の画像: `tools\紙_テスト_2026-09-23.png` / `tools\紙_練習_2026-09-23.png`（A4相当・print メディア）
- 道具: `tools\count_data.js` / `実機確認_選ぶ手間_2026-09-23.js` / `実機確認_対照_2026-09-23.js` /
  `実機確認_追加_2026-09-23.js` / `実機確認_紙を見る_2026-09-23.js` / `答えの露出をさがす.js`
- 実行: `$env:NODE_PATH="$env:APPDATA\npm\node_modules"; node <ファイル>`（playwright はグローバル導入ずみ）

### 次にやること
**司令塔の返事待ち。**返事が来るまで、`index.html` には手を入れない。

---

## 2026-09-23（2件目）｜依頼: テストプリントからヒント（意味）を消す
ユーザー判断（司令塔経由）: **「漢字のヒントはいらないです。出すとしても練習の紙に出してもらえれば。」**
→ ①テストは `mean` を出さない／②練習は出してよい。「同じ紙に載せない」案は取り下げ。

### 作る前の確認：読みだけで答えが決まるか
`tools\読みだけで決まるか.js`（自己テスト内蔵）で489語を機械で照合。
- **読みが同じなのに漢字が違う組: 0組**（読みの種類 463／同じ読み・同じ漢字の重複語 25語は害なし）
→ 司令塔の関門（「同じ読みの語が2つ以上ないか」）は**通過**。そのまま消した。

⚠️ **ただし「データの中で一意」と「子どもが一意に書ける」は別です**（司令塔へ報告ずみ・判断待ち）。
`tools\短い読みを見る.js 4` で短い読みを人が見たところ、**読みだけでは漢字を決めにくい語が9語**ある:

| 語 | 読み | 書きまちがえそうな字 | 回 |
|---|---|---|---|
| 租 | そ | 祖・組・素 | 第3回 |
| 庸 | よう | 用・様・洋 | 第3回 |
| 調 | ちょう | 町・長・朝・帳 | 第3回 |
| 唐 | とう | 東・島・答・刀 | 第2回 |
| 隋 | ずい | 随・髄 | 第2回 |
| 倭 | わ | 和・輪・話 | 第1回 |
| **竿燈** | かんとう | ★**関東** | 夏期復習編8 |
| **冷害** | れいがい | ★**例外** | 夏期復習編8 |
| 機械 | きかい | 機会・器械 | 夏期復習編3 |

（`平氏`(へいし) と `平治`(へいじ) は第4回で同居し、濁点1つ違い。これも要注意）
★**租・庸・調は3つとも第3回**にいるので、同じ紙に並びます。
なお②練習プリントを先にやる運用なら、お手本で見た字を書くことになるので実害は下がります。**判断は司令塔へ。**

### 直したもの（`index.html` のみ・データと記録は無変更）
- `renderTestPrint()`: `<span class="test-hint">(${it.mean})</span>` を削除。**テストは読みだけ**
- `.test-hint` の CSS を廃止（再発しないよう、消した理由をコメントで残した）
- `renderPracticePrint()`: お手本の行に `<span class="p-mean-sub">${it.mean}</span>` を追加＝**練習にヒントを移した**
- `.p-mean-sub` を追加（7.5pt。もとのテスト側ヒントは 6.5pt だったので、むしろ大きい）

### 検証（`tools\紙に答えが出ていないか.js`・★対照つき）
**直す前の版（`git show HEAD:index.html`）を一時フォルダに取り出し、同じ検査を先に当てて「鳴ること」を確かめてから本番を測る。**
鳴らなければ数字を出さずに終了コード3で止まる（確認ポイント 4-1／4-6）。

```
直す前（HEAD）… 紙41枚 / 答えが印刷されている紙 25枚 / のべ57語   ← 鳴った
いまの版      … 紙41枚 / 答えが印刷されている紙  0枚 / のべ 0語
練習プリント  … お手本10語 / 意味10件 / データと一致 true
```
- 紙の枚数が前後で41枚のまま＝**出題の選び方は変えていない**
- 通しの検査も再実行して全部 NG 0（`実機確認_選ぶ手間`／`実機確認_対照`／`実機確認_追加`）
- **目で見た**: `tools\紙_テスト_2026-09-23.png`（ヒント無し・第4回で「太政大臣」「平清盛」の漏れが消えた）
  `tools\紙_練習_2026-09-23.png`（お手本の右に意味が出ている。ページ数は前と同じ）

### 状態
- ローカルコミット `f556374`。**push はしていない**（公開の承認は司令塔からユーザーへ）
- 記録（`kq_kanji_stats_v1`）・語のデータ（`kanji-data.js`）・選び方の設定は一切触っていない

### ★公開ずみ（2026-09-23・ユーザー承認を司令塔経由で受領）
`4d1b301` を `origin/master` へ push（`47e6dff..4d1b301`、早送り。origin は 0 behind だった）。
GitHub Pages のビルド完了を待って、**公開URLを実際にブラウザで開いて**確認（`tools\公開版を確かめる.js`）。

```
https://nambanamba.github.io/kanji-print-app/
【① テスト】紙41枚 / 答えが印刷されている紙 0枚 / のべ0語 ／ 読みだけ: true
【② 練習】お手本10語 / 意味10件 / データと一致 true
```
紙の画像: `tools\公開版_紙_テスト_2026-09-23.png`（目視ずみ）

⚠️ **`kanji-data.js` の sha256 が公開版とローカルで食い違いました。原因は改行コードでした**（確認ポイント 0-3 ④）。
```
公開 / git blob … 148,129 byte・LF 5429・CRLF 0
ローカル        … 153,558 byte・CRLF 5429・LF 0   ← checkout 時に CRLF へ正規化されたもの
改行をそろえると完全一致。git でも 47e6dff..HEAD で差分なし ＝ データは無変更
```
⚠️ **あわせて、文字列一致での確認に誤検出が出ました。**公開版を `test-hint` で grep すると True になりますが、
これは**廃止したことを書いたコメントに文字列が残っている**ためです（確認ポイント 4-3b）。
→ **文字列で判定せず、実際に紙を描かせて中身を見る**ようにしました。

### ★添え書き（②）の文案 —— 司令塔へ提出ずみ・ユーザー確認待ち
持たせ方の提案: **アプリ側（`index.html`）に word をキーにした小さな表**を持つ。
- **`kanji-data.js` には足さない。**元データからの取り込みで作り直されるため、手で足した欄は次の取り込みで消える。
  また `quiz_csv` 担当の領分に入らずに済む
- **キーは `id` ではなく `word`。**id が振り直って記録が別の語に移った前例がある（2026-09-05 の項）

| 語 | 読み | 添え書き（★司令塔の指摘を反映した第2版） | 字数 |
|---|---|---|---|
| 倭 | わ | 昔の日本のよび名 | 8 |
| 隋 | ずい | 中国の王朝 | 5 |
| 唐 | とう | 中国の王朝 | 5 |
| 租 | そ | 稲で納める税 | 6 |
| 調 | ちょう | 特産物で納める税 | 8 |
| 庸 | よう | 布で納める税 | 6 |
| 竿燈 | かんとう | 夏祭りの名 | 5 |
| 冷害 | れいがい | 夏の低温で作物が不作 | 10 |
| 機械 | きかい | 工業の種類 | 5 |
| 平氏 | へいし | 武士団の名 | 5 |
| 平治 | へいじ | 1159年の乱の名 | 9 |

`tools\添え書き案を検査する.js`（自己テスト＋対照つき）で全件 OK。
★**現行の `mean` をそのまま短くして流用すると、6件が条件2（他の答えを含む）に引っかかります**——
隋←遣隋使／租←口分田／竿燈←秋田市／平氏←桓武天皇／平治←平清盛・源義朝。**書き下ろす必要があります。**

#### ⚠️★初版の検査に見落としがありました（司令塔の指摘・2026-09-23）
**条件1を「語まるごと」でしか照合しておらず、冷害「夏の低温の被害」の「害」を見逃して OK と出していました。**
**確認ポイント 4-3b の型**——こちらが①で踏んだ `test-hint` の誤検出と、向きが逆なだけで同じ話です。

直したこと:
- **1字ずつの照合（1b）を足した。**自己テストに**見落とした実例そのもの**（冷害「…被害」）を仕込み、鳴ることを確認
- **対照も足した。**直した案（「夏の低温で作物が不作」）に鳴らないことを確かめて、鳴りすぎていないことを示す
- 同じ単元の別の答えとの**1字重なり（2b）は参考にとどめ、NG にしない**。「税」「名」のような1字はどこにでも出るため

第2版で残った 2b（参考。人が見て判断する）:
`冷害`の「作」←輪作 ／ `機械`の「種」←種子島 ／ `平氏`の「武」←桓武天皇 ／ `平治`の「乱」←平将門の乱
いずれも1字だけで、その答えを当てられる形にはなっていないと判断しています。

#### 隋・唐 から年号を外した理由（司令塔の判断）
読みが「ずい」「とう」で違うので、**互いの区別に年号は要りません。**
むしろ**教材で習うのは遣隋使607年（7世紀）**なので、「6世紀の隋」と書くとそこで手が止まります。
→ 両方とも「中国の王朝」。文面は同じですが、読みが違うので困りません。

#### 下ごしらえ（司令塔の指示・2026-09-23）: 検査を添え書き対応にした
`tools\紙に答えが出ていないか.js` を改修。**`index.html` は触っていません。**

★**この検査は `.test-item` の `textContent` を丸ごと見ています。**
→ **添え書きを `.test-item` の中に入れれば、自動的に走査の対象に入ります。**
⚠️ **逆に、`.test-item` の外に置くと、この検査からは見えません。**実装するときの必須条件です。

入口の自己テストを3つに増やし、**1つでも落ちたら数字を出さずに終了コード3で止まります**（確認ポイント 4-6）。
```
(a) 添え書きから答えが漏れる偽の紙  → 鳴るべき   … 10件差しこみ／10語を拾った  OK
(b) 答えを含まない添え書きの偽の紙  → 鳴らぬべき … 0語                        OK
(c) 直す前の版（47e6dff）           → 鳴るべき   … 41枚中25枚・のべ57語        OK
本番: いまの版 41枚中 0枚 ／ 練習プリント 意味10件・データと一致
```
(a)(b) の仕込みは**データから作ります**。「隣の問題の答えを添え書きに書いてしまった紙」を組み立てる形で、
特定の語を名指ししていません（確認ポイント 4-6b）。

##### ⚠️★この改修で、自分のコミットが対照を壊していたのが見つかりました
旧版は比較先を **`git show HEAD:index.html`** にしていました。
**ヒントを消した修正を HEAD にコミットした瞬間、「直す前の版」が修正後の版になり、対照が鳴らなくなりました。**
実際に走らせたら `直す前（HEAD）… 0枚` と出て、検査は**数字を出さずに止まりました**（入口の自己テストが効いた形）。
→ 比較先を **`BEFORE_FIX = '47e6dff'` とコミットで固定**しました。
**「直す前」を HEAD で表してはいけません。**自分が直した瞬間に意味が変わります。

#### ★実装は保留中
ユーザーの指示は「**漢字のヒントはいらないです。出すとしても練習の紙に出してもらえれば**」でした。
**添え書きをテストの紙に入れてよいか**を司令塔がユーザーに確認中です。**回答が来るまで `index.html` に足さないこと。**
（練習の紙には `mean` がすでに出ているので、そちらに足しても意味がありません）

---

## 2026-09-23（3件目）｜添え書きを実装（★公開前・司令塔へ報告ずみ）
ユーザー承認: **「これは、よみだけでわからないものだけいれるんじゃないんでした？」**
＝ **読みだけでは答えが決まらない語にだけ付ける**で確定。
★**以前の「ヒントはいらない」と衝突しません。**あれは**易しくするヒント**の話、こちらは**問題が成立していない**話。別物。

### ★先にやった：11語に漏れが無いかの全数調べ（`tools\添え書きが要る語をさがす.js`）
489語ぜんぶに、機械で2つ当てました（自己テスト内蔵・入口で止まる形）。

```
① 1文字の語      … 7語（倭・隋・唐・調・庸・租 ＝承認ずみ6 ／ ★呉（くれ）＝11語の外）
② 読みの衝突      … 0組（漢字が違う同じ読みは無い。同じ語の単元またぎ25語は害なし）
判定: 承認ずみ11語からの漏れ ★1件 = 呉（くれ・夏期講習復習編4 kaki4_24・高）
```
★**`呉` の文案は作っていません。**司令塔が見る決まりなので、報告だけしてあります。**返事が来たら足すこと。**

⚠️ **この調べで0件でも「漏れなし」の証明にはなりません**（道具の先頭に明記）。
**同音の別語（データの外）は機械では見えません**——「れいがい→例外」「きかい→機会」「かんとう→関東」。
承認ずみ11語のうち**冷害・機械・竿燈の3語は、まさにその型で人が読んで見つけたもの**です。

### 直したもの（`index.html` のみ）
- **`TEST_NOTES`**（word キー・11語）を**スクリプトの外側**に置いた
  ⚠️ **最初 `renderTestPrint()` の内側に書いてしまい、外から見えず検査が動きませんでした。**関数の外に出して解決
- `makeTestItem()` が、表にある語のときだけ **`.test-item` の中に** `<span class="test-note">（…）</span>` を出す
- `.test-note` を追加（7.5pt）
- **`kanji-data.js` は触っていません**（取り込みで消えるため）

### 検証
| 道具 | 結果 |
|---|---|
| `紙に答えが出ていないか.js` | 自己テスト3つ OK ／ 直す前25枚・**いまの版 41枚中0枚** ／ NG 0 |
| **`添え書きが紙に出ているか.js`**（今回追加） | 11/11 出た ／ 表に無い語に付いていない ／ **全部 `.test-item` の中** ／ 租・庸・調が3つとも違う ／ NG 0 |
| `添え書き案を検査する.js` | NG 0 |
| `実機確認_選ぶ手間` / `実機確認_対照` | NG 0 |

★**「0枚」だけでは足りません。**何も足さなくても0枚になるので、
**「足したものが効いているか」を別の道具で見ています**（確認ポイント 0-3）。

目で見た: `tools\紙_第3回_添え書き_2026-09-23.png`
`1.そ（稲で納める税）／2.よう（布で納める税）／3.ちょう（特産物で納める税）`——
**租・庸・調が同じ紙に並んでも区別が付きます。**他の7問には添え書きが付いていません。

### ★公開ずみ（11語ぶん・2026-09-23）
`425991d` を push（`4d1b301..425991d`）。Pages のビルド後、**公開URLを実際に開いて紙を描かせて**確認。
```
https://nambanamba.github.io/kanji-print-app/
① テスト 41枚中0枚 ／ 読み＋（添え書き）だけ true
   添え書き: 表11語 → 紙に出た11語 ／ .test-item の外 0件 ／ 表に無い語に付いた 0件
② 練習   お手本10語・意味10件・データと一致 true
```

### `呉` を追加（★ローカルのみ・push 待ち）
司令塔から文案「造船がさかんな市」。**先に検査に当ててから**入れました。
- `造船` は**489語のどこにも語として登録されていません**（0件）→ 条件2a 通過
- `市` 単独の語も無し。`松江市` 等とは1字重なるだけ（2b の参考枠。既存4件と同じ扱い）
- 1a / 1b / 3 / 4 / 4b すべて OK
→ `TEST_NOTES` は **12語**に。全検査6本とも exit=0 / NG 0。

### ★この回で直した、検査そのものの不具合（3件）
すべて**アプリではなく検査側**の問題です。数字を信じる前に潰しました。

1. **条件4が「11語ちょうどか」と件数を決め打ち**していた（確認ポイント 4-6b 違反を自分の道具でやっていた）。
   `呉` を足した瞬間、中身は正しいのに NG。→ **件数ではなく「司令塔が承認した語の集合」と突き合わせ**に変更
2. ★**道具が `TEST_NOTES` の写しを持っていた。**`index.html` に `呉` を足しても、道具は11語のままで
   「まだ漏れている」と表示。→ **`tools\_添え書き表を読む.js` を作り、実物の `index.html` から読む**ようにした。
   読めなければ例外で止まります（空表を返すと「漏れ0件」で素通りするため）
3. **`公開版を確かめる.js` の「かなだけ」判定**が「番号＋読みだけ」を期待しており、
   **添え書きのある語が載った紙では、正しいのに NG になる**作りだった（確認ポイント 4-3）。
   → 「読み」または「読み＋（添え書き）」を通す形にし、あわせて**見る回を第3回に変更**。
   第3回の先頭10語には租・庸・調（添え書きあり）と無い語が混ざるので、**両方の枝を1枚で通せます**。
   ⚠️ 添え書きの無い回だけ見ていると、**出る枝を一度も試さずに OK が出ます**
   （同じ理由で `公開版を確かめる.js` の練習プリント判定も、`currentSet` を組み直さず「一致 false」を出していた。修正ずみ）

### 状態: ★12語すべて公開ずみ（`a3e0bcc`）
承認を受けて push（`425991d..a3e0bcc`）。Pages 反映後、**公開URLで実地確認**（`tools\公開版を確かめる.js`）。
```
① テスト 41枚中0枚 ／ 読み＋（添え書き）だけ true ／ ★添え書きが出る枝も通した（第3回で3件）
   添え書き: 表12語 → 紙に出た12語 ／ .test-item の外 0件 ／ 表に無い語に付いた 0件
② 練習   お手本10語・意味10件・データと一致 true        NG 0
```
目で見た紙（公開版）:
- `tools\公開版_紙_テスト_2026-09-23.png`（第3回。2.ちょう／3.よう／9.そ に添え書き）
- `tools\紙_呉_公開版.png`（夏期復習編4。**1. くれ（造船がさかんな市）**、他9問は添え書き無し）

道具を1つ足しました: **`tools\紙を1枚見る.js <語> [--public]`**
指定した語を含む紙を1枚描いて画像にするだけのもの（検査ではありません）。
**公開版とローカルのどちらも見られます。**次に語を足したとき、その1枚をすぐ目で確認できます。

---

### ★文書の扱いについて（2026-09-23・司令塔と合意）
- **確認用の画像・PDF・ログは git に載せない。**道具を走らせれば作り直せるため。
  ★**どのファイルをどの道具が作るかを、この作業メモに書いておく**のが条件
- `UI案_最重要10と正解日_2026-09-20.md` は **採用ずみ**（案Aが 47e6dff で実装・公開）。
  ⚠️ **ファイル名が「UI案_」で、長く未コミットだったため「未採用」に見えていました。**
  司令塔から「未採用・採否未決と書いて」と指示が来ましたが、**中身・作業メモ134行・コミットの3点を確かめて
  事実と違うと分かったので、書かずに差し戻しました。**司令塔が誤りを認め、いまの注記に直しています
  > ★**司令塔の指示でも、事実と違えば実行しないこと。**理由と代案を添えて返せば直してもらえます。
  > **文書は間違っていても何も鳴りません**（確認ポイント 0-3）。「未採用」と書いていたら、
  > 次の人は**公開ずみの機能を「まだ無い」と読んで作り直し**にかかっていました

### ★確認ポイントに入った項（この担当の事故が元。道具を直すときは守ること）
⚠️ **この表は「守っているものの一覧」であって、「道具の全件表」ではありません。**
**ここに載っていない道具にも役割があります。**載っていないことを「消してよい」と読まないでください
（道具の一覧と用途は、各ファイルの先頭コメントにあります）。

| 項 | 何を言っているか | どの道具で守っているか |
|---|---|---|
| **4-6b** | 検査の**対象**（回・番号・件数）を決め打ちしない → 条件で選ぶ | 全部 |
| **4-6c** | 検査の**対照**（直す前の版）は決め打ちする → コミットで固定（`BEFORE_FIX`） | `紙に答えが出ていないか.js` |
| **4-6d** | 検査は**出荷されるものそのもの**を見る。写しを持たない。★**読めなければ例外で止める**（空表を返すと「漏れ0件」で素通りする） | `_添え書き表を読む.js` |
| **4-6e** | 検査を当てる対象は、**分岐が両方通るもの**を選ぶ（添え書きが出る枝／出ない枝） | `公開版を確かめる.js` が第3回を見る理由 |

⚠️ **4-6b と 4-6e の住み分け**（司令塔が明記）:
**決め打ちしてよいのは「検査自身を試すための1枚」だけ。本番の走査対象は条件で選ぶ。**

### ★この2つの道具は、確認ポイントの参照実装に指定されました（司令塔・2026-09-23）
`tools\紙に答えが出ていないか.js` と `tools\添え書き案を検査する.js` が、
**`司令塔\確認ポイント.md` 4-6c** から参照実装として名指しされています。
> **検査の中で「前」「元」「正しいはず」を、動くもの（HEAD・最新・いま）で表さない。**
> - 検査の**対象**（回・番号・件数）は決め打ちしない → 条件で選ぶ（4-6b）
> - 検査の**対照**（直す前の版）は決め打ちする → コミットで固定する（4-6c）

⚠️ **この2つを書きかえるときは、次の形を崩さないでください。**他の担当が手本にしています。
- **(a) 鳴るべき偽物 と (b) 鳴らぬべき偽物を、対にする**（(a) だけなら網を広げて通せてしまう）
- **偽物はデータから組み立てる**（語・回・件数を名指ししない）
- **対照はコミットで固定する**（`BEFORE_FIX`）
- **入口に置き、落ちたら数字を出さずに止める**

### 状態（2026-09-23 時点の最後）: 待機中
- **ユーザーの回答待ち**: 「添え書きをテストの紙に入れる／入れない（入れないなら11語はテストから外す）」
- ⚠️ **来週が組分けテスト（社会・理科 第1〜4回）でユーザーの手はそちらに向いています。返事は遅れます。催促しないこと**（司令塔）
- 回答が来たら即やること: `index.html` の `renderTestPrint()` に、word をキーにした11語の表から添え書きを
  **`.test-item` の中に**出す。そのあと `紙に答えが出ていないか.js` を走らせて **41枚中0枚のまま**を確認 → コミット → 報告
- **新しい回・新しい機能を探さないこと**（共通 `CLAUDE.md` 0c）。待つのは消費ゼロ

### 保留（着手しないこと）
**「重要度の付け直し（厳選）」**。★週テストからの逆引きはしない方針（テストの答えを先に練習させるため・ユーザー指摘）。
教材の中だけで測る方針を司令塔が検討中。**指示が来るまで着手しない。**

---

## 2026-09-24（★最優先を親が付けられるようにする・着手）

**依頼**: `司令塔\回答\漢字プリント_親が優先度を変える_依頼_2026-09-24.md`（司令塔 `claude-ee` [6d5ae0]）
**担当交代**: 前任から引き継ぎ。名簿 `セッション名簿\kanji-print-app.md` を 09-24 付で更新ずみ。
⚠️ この環境の `ListAgents` は自分の行（`This session is …`）を返さないため、**自分の宛先を名乗れない**（前任と同じ）。

### 頼まれたこと（要点だけ。全文は依頼書）
- **語数で絞る案は取り下げ。489語も `priority` も触らない**
- ③一覧で親が語ごとに **★最優先** を付けられるようにする。★は印刷の先頭に置く＝必ず入る
- ★は **重要度フィルタで落とさない**／正解ずみを除く には従う（正解ずみの★は入らない）／
  「選んだ語だけ」では使わない／「出さない」が勝つ
- 保存は新しい箱 `kanji_app_star_v1`（picked/excluded と混ぜない）
- おすすめ110語を参考バッジで出す。**★の初期値にはしない**

### 起動時の確認（`CLAUDE.md` 0d）
- `git status` … clean。**未追跡の png/pdf が7つ**（`tools\`）。公開時に巻き込まない
- `git fetch` … `HEAD` = `origin/master` = **`9f89e1d`**。依頼書の対照コミットと一致

### ★最初にやった「止まるべきか」の確認 → 止まらなくてよい
おすすめ110語が `kanji-data.js` の id に全部当たるかを機械で当てた
（新しい道具 `tools\おすすめ110語を当てる.js`。出典の「## 全体の並び（110語）」の表を読む）。

```
出典の行数: 110（期待 110）
当たった: 110 / 当たらなかった: 0 / 同じ回に同じ語が複数: 0
回ごとの内訳: 第1回 27 / 第2回 20 / 第3回 20 / 第4回 43
```

- 心配されていた `竪穴住居`/`竪穴式住居` の表記ゆれは **無し**（出典もデータも `竪穴住居`）
- 内訳は出典が自分で書いている 27/20/20/43 と一致。出典の中の重複行も 0
- → **司令塔に止めて報告する条件（依頼書の失敗条件3）には当たらない。実装に進む**

### 次にやること
1. `★` の実装（`kanji_app_star_v1`・③一覧のUI・①のN語表示・おすすめバッジ）
2. 自己テスト付きの検査を作る（対照は **`9f89e1d` でコミット固定**。`HEAD` で表さない＝4-6c）
3. `tools\紙に答えが出ていないか.js` を通す（41枚 0枚の維持）
4. 司令塔に報告 → **push はユーザー承認後**

### 実装できました（ローカルのみ。★push は未実施・ユーザー承認待ち）

**司令塔から 09-24 に追加で2点もらいました。**（どちらも依頼書に無かったもの）
1. ★は**単元の選択を超えない**で正しい。①に「設定で今回出ない★が何語あるか」も併記すること
2. 「おすすめを全部★にする」で★110語になり「おすすめだけを回す」状態になるのは**意図した動き**
3. ★**0件のとき紙が変わらないことは、目でなく機械で `9f89e1d` と突き合わせて確かめること**

#### 触ったファイル（3つ）
- `index.html` … +233 / −12 行
- `README.md` … 特長に「6. ★最優先」を追加（旧6〜10を7〜11に繰り下げ）、保存キーの節に `kanji_app_star_v1` を追記
- `作業メモ.md`（これ）

#### 新しい道具（3つ・すべて `tools\`）
| ファイル | 何をするか |
|---|---|
| `★最優先を検査する.js` | ★**合否を決める検査。**入口の自己テスト3つ＋本番5項目 |
| `おすすめ110語を当てる.js` | 出典の110語を `kanji-data.js` の id に当てる（→ `おすすめ110語_当てた結果.tsv`） |
| `実機確認_★_2026-09-24.js` | 390px の実機クリック確認（→ 画像4枚＋結果txt） |

#### 作りの要点
- 保存は `kanji_app_star_v1`（`単元キー\u0000語`）。**picked/excluded とは別の箱**
- `getPool()` … ★は重要度で落とさない。★**0件なら `star.size > 0` の条件で素通り**＝直す前と同じ
- `generateDailySet()` … 並べ替え後に★を先頭へ寄せる。★**0件なら分割しても同じ並び**
- おすすめ110語は `RECOMMENDED_KEYS` として `index.html` に持つ（**検査は写しを持たず本体から読む**＝4-6d）

#### 検査の結果（4つとも通りました）
```
★最優先を検査する.js
  入口の自己テスト … (a)偽の実装=鳴った / (b)いまの実装=鳴らない / (c)9f89e1d=鳴った  → 3/3 OK
  対象に選んだ回: 夏期講習復習編3（高24語・高でない28語 ＝ 両方の枝が通る・4-6e）
  【1】「高だけ」で★3語すべて紙に出た。★でない「高でない」語が出た数 0  → OK
  【2】★0件で 132枚（11回×重要度3×正解ずみ2×語数2）を 9f89e1d と突合 → ちがった紙 0枚
  【3】全語★の状態で 11回×テスト/練習 の紙に ★・☆・おすすめ・最優先 が出た数 0
  【4】記録・出さない・チェックの3つとも、★操作の前後で完全一致
  【5】依頼書の表5項目（正解ずみ/出さない/選んだ語だけ/単元/語数超過の切り方）すべて OK

紙に答えが出ていないか.js  … 紙41枚 / 答えが出ている紙 0枚（維持）。NG合計 0
おすすめ110語を当てる.js   … 110/110 一致・表記ゆれ0・重複0
実機確認_★_2026-09-24.js  … 390px ではみ出し 0件。★0件で①の行は非表示（直す前と同じ画面）
```

#### ★検査を作るときに踏んだ罠（次の人へ）
★**「紙にその語が出ているか」を `word` で探すと、正しい実装でも必ず「出ていない」になります。**
**テストプリント（①）に刷られるのは読み（かな）だけで、語そのものは出ません**（出たら答えの露出）。
自己テスト(b) が鳴ってしまい、そこで気づきました。**①は読みで、②（練習）は語で見る**形に直しました。
⚠️ **自己テストの (b) が無ければ、「★が紙に出ない」という嘘の数字をそのまま報告していました。**

#### 判断したこと（司令塔に伝えて同意ずみ）
- **★は単元の選択を超えない。**代わりに①に「★N語のうち M語は今回出ません」と書く
- 「🚫 出さない」にした語には★**を付けられない**（外すほうは制限しない。掃除できるように）
- ついでに `.btn-sm.is-on` の CSS を足した。★**既存の「選んだ語だけ表示」も `is-on` を付けていたのに
  CSS が無く、押しても見た目が変わっていませんでした**（★だけ表示で同じことが起きるため）

#### 次にやること
- ★**司令塔の返事待ち → ユーザー承認が出たら push。**勝手に公開しない（確認ポイント 3-1c）
- ⚠️ 公開のときは `tools\` の**未追跡 png/pdf 7つ（09-23分）と、今回作った画像4枚＋tsv＋結果txt**の
  扱いを決めること。**巻き込まない／落とさない**（3-4c）。道具の .js 3つは入れる想定

### 09-24 追記: 司令塔が独立に検査を回し、同じ結果を確認（6-1）

司令塔側でも `★最優先を検査する.js` ほかを回し、**【1】〜【5】すべて OK / 紙41枚0枚 / 110分の110** を確認。
`git diff` も「`index.html`・`README.md`・`作業メモ.md` の3つだけ、`kanji-data.js` は無変更」で一致。

### 09-24 追記: 承認待ちの間にやった2つ

#### 1. `.gitignore` を新設（司令塔の判断）
★**道具（`.js`）は入れる。生成物（png・pdf・tsv・`*_結果.txt`）は入れない。**
- 生成物は**道具を走らせれば作り直せる**ので、リポジトリを重くするだけ
- **落として困らないことは確認ずみ**: `index.html` からこれらへの参照は0件（司令塔が実測）。
  3-4c が心配しているのは「アプリが本当に使う画像を落とす」型で、`tools\` の生成物は当たらない
- 効果: 未追跡が **17件 → 4件**（`.gitignore` と新しい道具3つだけ）になった。
  **本当に入れ忘れた物が埋もれない**
- 確認: 無視された13件は全部生成物。**無視された `.js` は0件**（`git check-ignore` で3つとも「入る」）

#### 2. playwright の注記を、playwright を使う道具11個の頭に入れた
司令塔の環境で `Cannot find module 'playwright'` で一度落ちたため。**落ちる当の行の真上**に置いた
（スタックトレースがその行を名指しするので、いちばん目につく）。
```
PowerShell : $env:NODE_PATH="$env:APPDATA/npm/node_modules"; node <ファイル>
Bash       : NODE_PATH=$(npm root -g) node <ファイル>
```

### ⚠️⚠️ ここで道具11個を一度に壊しました（次の人へ・これがこの日いちばん危なかった）

注記に `\npm\node_modules` と**バックスラッシュ**で書いたところ、
**シェルのヒアドキュメント（`<<'EOF'` の引用符つき）がバックスラッシュを食べて、`\n` が本物の改行になりました。**
コメント1行が3行に割れ、★**11ファイル全部が構文エラーになりました。**

- **気づけた理由は `node --check` を全ファイルに当てたからです。** 見た目では分かりません
- **直し方**: パスを `/` 区切りに書きかえた（`$env:APPDATA/npm/node_modules`）。
  PowerShell も Node も `/` で通るので、**バックスラッシュを書かなければ二度と割れません**
- ★**同じ罠を、この日3回踏んでいます**:
  1. `\u0000` → **本物のNULバイト**がテキストファイルに入った（`おすすめ110語_キー.txt`。作り直した）
  2. Python の置換文字列の中でも同じことが起き、`assert` が落ちた（`raw string` で回避）
  3. 今回の11ファイル

> ★**決めごと: このプロジェクトでファイルを書くとき、内容にバックスラッシュを入れない。**
> パスは `/` で書く。どうしても `\` が要るなら、**ヒアドキュメントを使わず `Write` ツールで書く。**
> そして★**書いたあとは必ず機械で検める**（`.js` なら `node --check`、テキストなら NUL バイトの有無）。
> **「書けたつもり」が黙って壊れます。エラーは出ません**（`CLAUDE.md` 0-3 と同じ型）。

全19ファイルの `node --check` を通しなおし、検査3つも再実行して**同じ結果**を確認ずみ。

### いまの状態
- ★**push はしていません。**ユーザーの承認を司令塔が聞いている最中
- 未追跡: `.gitignore` / 新しい道具3つ。変更: `index.html`・`README.md`・`作業メモ.md`・道具11個（注記のみ）

### ★2026-09-24 これを書いている時点の状態：待機中（何を待っているか）

★**待っているもの: ユーザーの「公開してよい」の承認。**
★**誰から来るか: 司令塔（`四谷大塚` [6d5ae0] ＝ 依頼書の `claude-ee`）から連絡が来ます。**
★**それまで push しません。**ユーザーが直接ここに承認を出した場合も、司令塔に一報すること。

#### 引き継ぐ人がすぐ動けるように

**作業は全部終わっています。残っているのは push だけです。**承認が出たら:
1. `git status` で、未追跡が **`.gitignore` と道具3つの4件だけ**であることを確かめる
   （生成物13件は `.gitignore` で無視ずみ。★もし17件に戻っていたら `.gitignore` が入っていない）
2. 変更を commit（`index.html` / `README.md` / `作業メモ.md` / 道具11個の注記 / 新規 `.gitignore` と道具3つ）
3. push → 公開版で `tools/公開版を確かめる.js` を回す

#### 検査の回し方（毎回これで通っています）
```
NODE_PATH=$(npm root -g) node "tools/★最優先を検査する.js"      → 自己テスト3/3、【1】〜【5】OK
NODE_PATH=$(npm root -g) node "tools/紙に答えが出ていないか.js"  → NG合計 0（41枚 / 0枚）
NODE_PATH=$(npm root -g) node "tools/おすすめ110語を当てる.js"   → 110/110 一致
NODE_PATH=$(npm root -g) node "tools/実機確認_★_2026-09-24.js"   → 390px はみ出し0件
```

#### 司令塔が独立に確かめたこと（2026-09-24・6-1）
- 検査3つを司令塔側でも再実行 → **同じ結果**
- `tools/` の差分 9ファイル 37行追加1行削除 → ★**コメント以外は1行も変わっていない**（機械で確認）
- `node --check` 19本すべて通過／無視された `.js` は0本／未追跡4件

#### ★`確認ポイント.md` が1か所直りました（司令塔が 0-3 の表を更新）
★**元の表の「`<< 'EOF'` と引用符でくくれば潰れない」は誤りでした。引用符でくくっても潰れます。**
**司令塔も同じ日に踏んでいます**（宛先の `uds:` のパスをファイルに書いたらバックスラッシュが減った）。
→ **間違った安心を与える一行だったので、今回の実例で置きかえられました。**
**このメモの上のほうに書いた「バックスラッシュを入れない／書いたら機械で検める」は、そのまま有効です。**

#### ★手が空いたからといって、次の回や次の機能に着手しないこと（`CLAUDE.md` 0c）
**司令塔から「追加で何かをする必要はありません」と明示されています。**
**待つのは消費ゼロです。**新しい仕事を探すより、止まっているほうが価値があります。

## ★2026-09-24 公開しました（ユーザー承認あり・司令塔が伝達）

**ユーザーの言葉: 「公開してください」。**司令塔から伝えられて実行しました。

### コミット `9f89e1d..b974200`
```
e9ab1ee 親が「必ず出す語」に★最優先を付けられるようにする
b974200 tools/ の生成物を git に入れないようにし、道具に NODE_PATH の注記を足す
```
16ファイル。**生成物は0件、未追跡の残りも0件**（出す前に `git status` で確認）。

⚠️ **この環境には git の user.name/user.email が設定されていませんでした。**
過去のコミットに合わせて `git -c user.name="Claude" -c user.email="noreply@anthropic.com"` を
**コマンドごとに付けて**commit しました（設定ファイルは書き換えていません）。
★**次の担当も同じところで止まります。**同じやり方で通してください。

### ★出したあと、公開URLで確かめたこと（0-4「直した」と「届いた」は別）

**出す前に** `git fetch` で `origin/master` が `9f89e1d` のまま（＝検査の対照と同じ）であることを確認。
**GitHub Pages のビルドが `built`（sha=b974200・error なし）になるのを待ってから**確認しました。

★**1回目は「届いていません」で止まりました。**Pages がまだ building 中だったためです。
道具の頭に「★の仕組みが届いているか」の門を置いてあるので、**空振りの確認を OK と報告せずに済みました。**

`node tools/実機確認_★_2026-09-24.js --public`（390px・公開URL）:
```
見ているもの: ★公開版 https://nambanamba.github.io/kanji-print-app/index.html
  届いたか: 関数=true / おすすめ=110語 / ①の行=true / ★だけ表示ボタン=true
【①】★0件のとき ①の★の行は見えない（＝直す前と同じ画面）
【③】☆を2つタップ → ★になり 2語 保存された（下北半島・陸奥湾）
【④】「★だけ表示」→ 行2・ボタンON
【⑤】①に「★最優先 2語が先に出ます。」が出た
【⑥】重要度「高だけ」にしても 2語 のまま（★が落ちていない）
【⑦】選んでいない回の★ → 「★3語のうち 3語は…今回は出ません」と併記された
【⑧】「選んだ語だけ」→「この間は★最優先は使いません」
【⑨】「おすすめを全部★にする」→ 110語ぶんが★になった
【⑩】390px ではみ出し 0件
【⑪】「★をすべて外す」→ 0語／①の★の行が消えた（＝今までどおりの画面に戻る）
```
`node tools/公開版を確かめる.js`: **NG合計 0**（公開版の紙41枚・答えが出ている紙0枚を維持）。

**目でも見ました**（画像は `.gitignore` で git には入れていません。作り直せます）:
- `公開版_★一覧_390px.png` … ☆/★ ボタン、★の行の色づき、おすすめバッジ、ボタン5つの折り返し
- `公開版_★印刷タブ_390px.png` … 語数の設定のすぐ下に「★最優先 2語が先に出ます。」

### 道具に足したもの（公開前にコミットずみ）
`実機確認_★_2026-09-24.js` に **`--public`** を足しました。公開版を見るときはこれを使います。
★**冒頭に「★の仕組みが届いているか」の門があり、届いていなければ数字を出さずに止まります。**

### いまの状態: 待機
**次の指示が出るまで、別の回・別の件に手を出さないこと**（`CLAUDE.md` 0c）。

---

## 2026-09-25（練習した回数を出す・着手）

**依頼**: `司令塔\回答\漢字プリント_練習した回数を出す_依頼_2026-09-25.md`
**起動時確認**: `git fetch` 後 HEAD = origin/master = **`d6f7cb8`**（依頼書の対照コミットと一致）。作業ツリー clean。

### 頼まれたこと
★**練習プリント（②）に出した語だけ**を数える。**テスト（①）では数えない。**
理由はユーザーの言葉: **テストでまちがえた語は多くても、実際に書いて練習するのは4語くらい**。
「まちがえた数」だと**お子さんが実際にやった量と合わない**。★**数えたいのは「書いた回数」。**

- 保存は新しい箱 `kanji_app_practice_count_v1`（記録・★・チェック・除外と混ぜない）
- ③一覧の各行に「練習 N回」。★**0回の語には何も出さない**
- 過去の分は無い。**0から貯まる**旨を画面に1行

### ★司令塔から先にもらった事実（自分でも確かめた）
`setKanjiStatus` の分岐は unseen / mastered / weak の3つだが、画面から呼ばれるのは
**mastered と unseen の2つだけ**。★**`weak`（wrong:1）を呼ぶ導線が無い。**
さらに mastered は `wrong: 0` で上書きするので、★**`wrong` を読むと必ず0になる。**
→ **`wrong` は使わない。新しい箱に自分で数える。**

### ★数えどきをどうするか（自分で決めてよい、と言われた部分）

**決めたもの: 「② 練習プリントを印刷」を押したとき。ただし〈その組み合わせ〉×〈その日〉につき1回だけ。**

- **押した場所で数える**（`printPracticeSheet()`）。★**`renderPracticePrint()` の中では数えない**
  - ① テスト印刷・プレビュー・**検査の道具が紙を描くとき**は、どれも通らない
  - ★検査は練習プリントを何十枚も描くので、**描画側で数えると検査が数を汚します**
- **同じ日に同じ組み合わせを刷り直しても増えない**（失敗条件2）
- **日が変わって同じ10語をまた刷ったら増える** ＝ 新しい練習なので、これは増えるべき
- 🔄 で別の組み合わせにして刷れば、同じ日でも増える（実際に2回書いているため）

**なぜ「印刷のたび」にしないか**: 紙詰まりで刷り直すのは1回の練習。
**なぜ「セットにつき1回」だけにしないか**: 翌日また同じ10語を書いたら、それは2回目の練習だから。

### 次にやること
1. 実装（`kanji_app_practice_count_v1`・③の表示・数え始めた日の1行）
2. 検査（対照は **`d6f7cb8` でコミット固定**・自己テスト3つ・数0で紙が変わらないことを機械で）
3. 司令塔に報告 → ★**push はユーザー承認後**

### 実装できました（ローカルのみ。★push は未実施・ユーザー承認待ち）

**司令塔が数えどきの案を承認**（2026-09-25）。3つの理由とも正しい、とのこと。
とくに「★描画側で数えると検査が数を汚す」は**依頼書で見落としていた点**だと言われました。
`renderPracticePrint()` で数えていたら、**検査を1回走らせるたびに練習回数が何十回も増えていました。**

司令塔からの追加: 「〈組み合わせ〉×〈日〉の鍵が端末にたまり続ける形なら、古いものを落とすこと」。
→ ★**もともと手当てずみです。**`done` は `{ ymd, sigs }` で、**日が変わったら丸ごと捨てます。**
　 指紋も FNV-1a の短い文字列なので、全問（489語）の紙でも鍵は短いまま。**回数そのものは消しません。**

#### 触ったファイル
- `index.html` … 保存キー `kanji_app_practice_count_v1`／数える仕組み／③の「練習 N回」／説明のまとめ直し
- 新しい道具2つ: `tools/練習した回数を検査する.js`・`tools/実機確認_練習回数_2026-09-25.js`

#### 作りの要点
- ★**数えるのは `printPracticeSheet()`（押した場所）。`renderPracticePrint()` の中ではない**
  - ① テスト印刷・プレビュー・**検査の描画**は、どれもそこを通らない
- 〈紙の指紋〉×〈その日〉で1回だけ。**同じ日の刷り直しは増えない／日が変われば増える**
- 中身 `{ v, since, counts: {語キー: 回数}, done: { ymd, sigs } }`。キーは `単元キー + NUL + 語`
- ⚠️ **`wrong` は読んでいません**（必ず0になるため。司令塔の確認どおり）

#### 検査の結果（4つとも通りました）
```
練習した回数を検査する.js
  自己テスト … (a)刷り直しで二重に数える偽の実装=鳴った / (b)いまの実装=鳴らない / (c)d6f7cb8=鳴った → 3/3
  【1】テスト印刷・プレビューのあと 0 → 0（増えない）
  【2】練習1回=10 → 刷り直し2回のあとも 10 ／ 別ぞろえは増える ／ 日が変わると 10→20
  【3】数0で 132組（テスト＋練習＝264枚ぶん）を d6f7cb8 と突合 → ちがった紙 0組
  【4】全語に「37回」を入れても、紙に数が出た枚数 0
  【5】記録・★・チェック・出さない は、3回ぶん数えたあとも完全一致

★最優先を検査する.js     すべて通過（前回ぶんの回帰なし）
紙に答えが出ていないか.js NG合計 0（41枚 / 0枚）
実機確認_練習回数_…js    390px ではみ出し0件。紙には数が出ていない
```

### ★自分で見つけて直した2つ

#### 1. 説明が画面の44%を占めていた（390px）
「練習 N回」の説明を足したら、③の説明の箱が**3つ**になり、**合計368px（画面844pxの44%）**に。
★**③を開いても語が1行も見えないまま始まっていました。**
→ **4つの印の説明を1つにまとめ、`<details>` でたたみました。**
```
たたんだ高さ 56px（前は3箱で368px）
最初の語「大宝律令」が y=683px → ★開いた時点で画面に見える（前は画面の外）
開けば4つの印の説明が全部読める（399px）
```

#### 2. ★そのせいで、自分の検査が黙って空振りになっていた（4-6d）
説明を `.hist-note` から `<details class="hist-guide">` に移したところ、
**実機確認の道具が `.hist-note` を見たままで「案内の文: (無し)」と出し、素通りしていました。**
★**「見つからなければ (無し) と書く」作りだったので、エラーにならずに何も確かめなくなっていました。**
→ ★**「見つからなければ鳴って止まる」形に変えました。**開いたときに4つの印がそろうかも見ています。
> **同じ型を2日続けて踏んでいます**（09-24 は「語で紙を探して必ず外れる」）。
> ★**画面の作りを変えたら、その画面を見ている検査を必ず走らせて、数字が変わっていないかを見ること。**

### 次にやること
- ★**司令塔に報告 → ユーザー承認が出たら push。**勝手に公開しない
