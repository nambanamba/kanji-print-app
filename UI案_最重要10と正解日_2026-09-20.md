# 漢字プリント｜「出す語を親が選ぶ」「正解した日だけ入れる」画面案（2026-09-20）

> **2026-09-20 に提出した案。案Aが採用され、47e6dff で実装・公開ずみ（2026-09-23 確認）。**
> **記録として残すもので、これから作るものではありません。**

担当: 漢字プリント担当 claude-c8。依頼書: `司令塔\回答\漢字プリント_最重要10と正解日のUI_依頼_2026-09-20.md`
**これは案です。実装はユーザーが選んでから。**

## 0. 今こうなっています（調べた結果）

- **語のデータ**: 全489語。1語につき 漢字・読み・意味（ヒントになる1行）・回・重要度・難易度 を持っています。
  **意味は全語に入っているので、一覧に出せます**（いまは画面に出していません）
- **記録の保存**: `kq_kanji_stats_v1` に「正解の回数・まちがいの回数・次にいつ出すか・最後に正解した日時」。
  **このキーは姉妹アプリ（対戦クイズ）と共有していて、名前を変えると過去の記録が読めなくなります**
  （過去に4回それで記録が消えています）。**案はどれもキー名を変えません**
- **プリントの組み方**: 回を選ぶ → 重要度などで絞る → **最後に正解した日が古い順に並べて、上から10語** を自動で取る。
  いまは「この語を出す」と名指しはできません（「🚫 出さない」で消す方向だけあります）
- **「出さない」設定**: すでに「回＋語」で保存されています。**データが更新されても消えない作りなので、今回の「選ぶ」もこれと同じ持ち方にします**

## 1. キーワードの一覧（画面①）

いまの「進捗一覧」を作り直します。

```
[回をえらぶ: 第4回 ▼]              第4回.平安時代 46語 / 選んだ 10語
────────────────────────────────────────────
☑  桓武天皇    かんむてんのう     平安京に都を移し…      正解 9/14
☐  平将門の乱  たいらのまさかどのらん  10世紀前半、関東で…   ―
☑  摂関政治    せっかんせいじ     藤原氏が…             ―
────────────────────────────────────────────
[この回を全部つける] [この回を全部はずす] [選んだ語だけ表示]
```

- 漢字・読み・意味の1行・正解した日 を並べます
- 回は**1つだけ選ぶ形**にします（46語なら1画面で見渡せます）
- 「🚫 出さない」ボタンは今のまま残します（消したい語はこれまでどおり）

## 2. プリントに出す語を選ぶ（画面②＝印刷の画面）

**チェックした語だけを出す**ための切りかえを、印刷画面の先頭に置きます。

```
出す語:  ●選んだ語だけ（10語）   ○いままでどおり自動でえらぶ
         ↑ふだんはこちら
[テストを印刷] [練習用を印刷]
```

- **「選んだ語だけ」にすると、重要度や語数の設定は使いません。**チェックした語がそのまま印刷されます
- 選んだ内容は保存します（`kanji_app_picked_v1`。回＋語で覚えるので、データ更新で消えません）
- **「いままでどおり」も残します。**今の使い方をしている人（お子さんの自習）が困らないようにするためです

## 3. 正解した日を入れる（画面③）

★ここだけ、**案が2つ**あります。どちらか選んでください。

### 案A（おすすめ・かんたん）「正解した日」を1つだけ持つ
```
桓武天皇   かんむてんのう      [ 2026-09-20 ] [✓ 正解にする]   最後の正解 9/14
```
- 日付の欄は**その日が最初から入っています**。押すだけで今日の記録になります
- 前の日にさかのぼりたいときは、日付を変えてから押します
- **⭕❌の2つボタンは、⭕だけにします。**まちがいは入れません
- 保存は今の場所のまま。「正解1回・まちがい0回・その日に正解」として書きます
- **取り消しもできます**（押し間違い用に「取り消す」を小さく置きます）

### 案B 正解した日を「全部」ためる
- 9/6・9/13・9/20 …と**履歴が残り**、「3回できた」が分かります
- 保存は別の場所（`kq_kanji_correct_dates_v1`）に足します。**今の記録には触りません**
- そのぶん画面は少し複雑になります

> **おすすめは案A**です。依頼の「正解した日だけ入れられる」に必要なのはA、
> Bは「何回できたか」を見たいときに要るものです。**あとからBに足すこともできます**（Aで書いた日付はBの1件目になります）

## 4. 今の記録について（消しません）

- **これまでの⭕❌の記録は、そのまま残します。**消す処理は入れません
- **今まで付いていた「まちがい」の記録も消しません。**ただし**画面には出さなくなります**
  （「苦手な問題だけ」の絞り込みは、まちがいを入れなくなると育たないため、いっしょに隠す案です）
- ★**ここだけ確認したいこと**: **「苦手な問題だけ」の絞り込みを、残しますか／隠しますか。**
  隠しても、記録自体は消えないので、あとで戻せます

## 5. 作業の順番（承認が出たら）

1. 画面①（一覧）→ ②（選んで印刷）→ ③（正解の日）の順に作る。①だけでも使えます
2. ローカルにコミットして一報。**push はユーザー承認後**
3. 既存の smoke-test が通ることを確認（壊して鳴る確認はしない・09-15 方針）
